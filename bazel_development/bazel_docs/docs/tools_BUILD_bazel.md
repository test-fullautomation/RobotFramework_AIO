# BUILD.bazel

**Pfad:** `tools\BUILD.bazel`  
**Absoluter Pfad:** `C:\workplace\ROBFW\components\bazel_aio\tools\BUILD.bazel`  
**Typ:** Bazel Build File (definiert Targets und Dependencies)

---

## Inhalt

```python
# Einfachere Alternative: Shell-Script Generator ohne Custom Rule

load("@bazel_skylib//rules:write_file.bzl", "write_file")
load("@rules_python//python:defs.bzl", "py_binary")

# Helper tool to generate test runner files
py_binary(
    name = "generate_test_runner",
    srcs = ["generate_test_runner.py"],
    visibility = ["//visibility:public"],
)

# ============================================
# Windows Batch Script
# ============================================

write_file(
    name = "run_all_tests_bat_content",
    out = "run_all_tests_generated.bat",
    content = [
        "@echo off",
        "REM Auto-generated test runner",
        "REM Generated by Bazel - no Python needed!",
        "",
        "setlocal enabledelayedexpansion",
        "",
        "set BAZEL_EXE=%BAZEL_EXEC%",
        "if \"%BAZEL_EXE%\"==\"\" set BAZEL_EXE=bazel",
        "",
        "set TOTAL=0",
        "set PASSED=0",
        "set FAILED=0",
        "",
        "echo ======================================================================",
        "echo Bazel Test Runner (Auto-Generated)",
        "echo ======================================================================",
        "echo.",
        "",
        "REM Run each test target",
        "",
        # Test 1
        "set /a TOTAL+=1",
        "echo [1/3] Testing: //python-jsonpreprocessor/test/pytest:execute_py_test_jpp",
        "\"%BAZEL_EXE%\" test --test_output=errors //python-jsonpreprocessor/test/pytest:execute_py_test_jpp",
        "if errorlevel 1 (set /a FAILED+=1) else (set /a PASSED+=1)",
        "",
        # Test 2 (Beispiel)
        "set /a TOTAL+=1",
        "echo [2/3] Testing: //python-extensions-collection:test",
        "\"%BAZEL_EXE%\" test --test_output=errors //python-extensions-collection:test",
        "if errorlevel 1 (set /a FAILED+=1) else (set /a PASSED+=1)",
        "",
        # Test 3 (Beispiel)
        "set /a TOTAL+=1",
        "echo [3/3] Testing: //robotframework-testsuitesmanagement:test",
        "\"%BAZEL_EXE%\" test --test_output=errors //robotframework-testsuitesmanagement:test",
        "if errorlevel 1 (set /a FAILED+=1) else (set /a PASSED+=1)",
        "",
        "echo.",
        "echo ======================================================================",
        "echo SUMMARY",
        "echo ======================================================================",
        "echo Total:  %TOTAL%",
        "echo Passed: %PASSED%",
        "echo Failed: %FAILED%",
        "echo ======================================================================",
        "",
        "if %FAILED% GTR 0 exit /b 1",
        "exit /b 0",
    ],
)

# ============================================
# Linux Shell Script
# ============================================

write_file(
    name = "run_all_tests_sh_content",
    out = "run_all_tests_generated.sh",
    content = [
        "#!/bin/bash",
        "# Auto-generated test runner",
        "# Generated by Bazel - no Python needed!",
        "",
        "set -e",
        "",
        "BAZEL_EXE=${BAZEL_EXEC:-bazel}",
        "",
        "TOTAL=0",
        "PASSED=0",
        "FAILED=0",
        "",
        "echo \"======================================================================\"",
        "echo \"Bazel Test Runner (Auto-Generated)\"",
        "echo \"======================================================================\"",
        "echo \"\"",
        "",
        "# Run each test target",
        "run_test() {",
        "    ((TOTAL++))",
        "    echo \"[$TOTAL] Testing: $1\"",
        "    if \"$BAZEL_EXE\" test --test_output=errors \"$1\"; then",
        "        ((PASSED++))",
        "        echo \"✓ PASSED: $1\"",
        "    else",
        "        ((FAILED++))",
        "        echo \"✗ FAILED: $1\"",
        "    fi",
        "    echo \"\"",
        "}",
        "",
        "# Test targets",
        "run_test //python-jsonpreprocessor/test/pytest:execute_py_test_jpp",
        "run_test //python-extensions-collection:test",
        "run_test //robotframework-testsuitesmanagement:test",
        "",
        "echo \"======================================================================\"",
        "echo \"SUMMARY\"",
        "echo \"======================================================================\"",
        "echo \"Total:  $TOTAL\"",
        "echo \"Passed: $PASSED\"",
        "echo \"Failed: $FAILED\"",
        "echo \"======================================================================\"",
        "",
        "[[ $FAILED -gt 0 ]] && exit 1",
        "exit 0",
    ],
)

# ============================================
# Executable Targets
# ============================================
# Removed - now using test_suite_runner.bzl macro instead


```

---

## Datei-Informationen

- **Größe:** 4910 bytes
- **Zeilen:** 134
- **Verzeichnis:** `C:\workplace\ROBFW\components\bazel_aio\tools`

---

## Navigation

**Übergeordnetes Verzeichnis:** `tools`

